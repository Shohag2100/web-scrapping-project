Analyze my Django video streaming implementation and fix performance and video optimization issues without changing existing API endpoints, URL patterns, request formats, response formats, serializers, or frontend integration.

Current implementation details:

* Videos are uploaded and stored successfully.
* HLS generation is handled by ensure_hls_generated().
* Download endpoint redirects to playlist.m3u8.
* FFmpeg is currently executed from ensure_hls_generated().
* Video metadata currently stores only duration.
* Existing business logic and permissions must remain unchanged.

Problems to solve:

1. HLS generation currently happens during video download requests, causing slow response times for large videos.
2. Large videos are not optimized properly before HLS generation.
3. FFmpeg commands are missing proper compression settings.
4. No processing status tracking exists.
5. Large video handling is inefficient.
6. HLS generation should support production-scale video streaming.

Requirements:

* Keep all existing APIs unchanged.
* Keep all existing routes unchanged.
* Keep serializer outputs unchanged.
* Keep download endpoint behavior unchanged.
* Do not redesign the application.
* Make minimal code changes required.

Implement:

1. Move HLS generation from request lifecycle to Celery background tasks.

2. Add video processing status:

   * pending
   * processing
   * completed
   * failed

3. Improve FFmpeg commands:

   * H.264 video codec
   * AAC audio codec
   * CRF-based compression
   * faststart optimization
   * optimized HLS segment generation

4. Detect source video resolution using ffprobe.

5. Generate adaptive HLS:

   * 1080p if source >= 1080p
   * 720p if source >= 720p
   * 480p for all videos
   * Never upscale videos

6. Store metadata:

   * width
   * height
   * bitrate
   * file_size
   * duration

7. Improve temporary file cleanup.

8. Improve logging and error handling.

9. Review the existing ensure_hls_generated() function and identify:

   * bottlenecks
   * memory issues
   * FFmpeg issues
   * scalability problems

10. Fix incorrect HTTP headers and streaming-related issues.

Output:

* Root cause analysis
* Exact files requiring modification
* Full code changes
* Celery task implementation
* Updated FFmpeg commands
* Model changes
* Migration files
* Production recommendations

Do not create new APIs. Do not modify frontend contracts. Focus only on fixing performance, optimization, and streaming issues in the current implementation.
_________________________________________________
__________________________________________________
I need help refactoring my Django + DRF subscription/access control system.

Current subscription plan model:

{
"id": 1,
"code": "free",
"product_id": null,
"name": "Free",
"price": "0.00",
"duration_days": null,
"is_unlimited_shorts": false,
"is_unlimited_series_video": false,
"is_unlimited_ai_chatbot": false,
"shorts_daily_limit": 3,
"series_trailers_daily_limit": 3,
"ai_chatbot_daily_limit": 3,
"description": "Free plan with daily limits",
"is_active": true
}

Business Rules:

* Free users can watch:

  * 3 shorts per day
  * 3 series trailers per day
  * 3 AI chatbot requests per day
* Limits reset daily.
* Premium users may have unlimited access depending on plan settings.
* Limits must be enforced from the backend, not the mobile app.

Current Problems:

1. `/api/subscriptions/check-access` reduces the remaining count every time it is called.
2. Access-checking and usage-consumption logic are mixed together.
3. Users can uninstall and reinstall the app and continue using free limits because tracking is not properly enforced.
4. Counts should never be stored only on the device.
5. Daily usage tracking is unreliable.

Required Solution:

* `check-access` must be READ ONLY.
* Calling `check-access` should never create, update, or deduct usage records.
* Usage should only be deducted when the user actually:

  * Starts watching a short
  * Starts watching a trailer
  * Sends an AI chatbot message
* Store all usage in the database.
* Track daily usage per user and per feature.
* Daily limits must automatically reset based on date.
* User uninstalling/reinstalling the app must not reset usage because limits are tied to the user account.
* Prevent duplicate counting if the same request is retried multiple times.
* Use database transactions and race-condition protection.

Please review my existing code and provide:

1. Proper database schema for daily usage tracking.
2. Django models.
3. DRF serializers.
4. Service-layer implementation.
5. API view refactor.
6. Migration examples.
7. Access-control logic.
8. Daily reset logic.
9. Best practices for Google Play and Apple subscriptions.
10. Production-ready code.

Suggested implementation:

Create a UserDailyUsage model:

* user
* date
* shorts_used
* trailers_used
* ai_chatbot_used
* created_at
* updated_at

Example:

2026-06-17

shorts_used = 2
trailers_used = 3
ai_chatbot_used = 1

check-access response should be:

{
"allowed": true,
"feature": "shorts_watch",
"plan": "free",
"used": 2,
"remaining": 1,
"daily_limit": 3
}

No counters should change when this API is called.

Only actual content consumption should update usage counters.

Please identify all bugs in the current implementation and refactor it according to clean architecture and DRF best practices.
